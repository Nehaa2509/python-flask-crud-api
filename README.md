# Flask User Management REST API

## 📌 Objective
This project is a simple REST API built using Flask.  
It performs CRUD (Create, Read, Update, Delete) operations on user data.

---

## 🚀 Technologies Used
- Python 3.x
- Flask
- Postman / Curl (for API testing)

---

## 📂 Project Structure
